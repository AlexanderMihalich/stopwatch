import React from 'react'

const Buttons = () => {
	return (
		<div>
			<button>Start</button>
			<button>Stop</button>
			<button>Reset</button>
			<button>Wait</button>
		</div>
	)
}

export default Buttons